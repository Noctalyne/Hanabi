{% extends 'base.html.twig' %}

{% block title %}Ajouter une adresse{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('./rework_css/adresses.css') }}">
    <link rel="stylesheet" href="{{ asset('./rework_css/account.css') }}">
{% endblock %}


{% block body %}

    <div class="container bloc">

        {% if msg != "" %}
            <div class='container text-center'>
                <div class="alert alert-dismissible alert-danger">
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    <strong>{{ msg }}</strong>
                </div>
            </div>    
        {% endif %}    



        {# Bloquer sur la page du compte user à 3 donv peut être inutile ici

            {% if limite < 3 %}
                {{ include('adresses/_form.html.twig') }}
            {% endif %}
        #}

        <div class="bloc_card">

            <div class="bloc_card_header">
                <h4> Ajouter une adresse </h4>      
            </div>

            {{ form_start(form) }}

                <div class="bloc_card_add">
                    <p>Numéros</p>
                    {{ form_row( form.numAdrss, {label: ' '} ) }}  
                </div>

                
                <div class="bloc_card_add">
                    <p>Rue</p>
                    {{ form_row( form.rueAdrss, {label: ' '} ) }}
                </div>

                
                <div class="bloc_card_add">
                    <p>
                        Complément d'addresse <br>
                        <small>(Résidence, Batiment, Appartement)</small>
                    </p>                    
                    {{ form_row( form.complementAdrss, {label: ' '} ) }}
                </div>

                
                <div class="bloc_card_add">
                    <p>Ville</p>
                    {{ form_row( form.villeAdrss, {label: ' '} ) }}
                </div>

                
                <div class="bloc_card_add">
                    <p>Code postal</p>
                    {{ form_row( form.codePostalAdrss, {label: ' '} ) }}
                </div>

                <div class="bloc_card_add">
                    <p>Pays</p>
                    {{ form_row( form.paysAdrss, {label: ' '} ) }} 
                </div>

                
                <button>
                    {{ button_label|default('Enregistrer') }}
                </button>            
            
            {{ form_end(form) }}

        
        </div>

        <button>
            <a href="{{ path('app_user_show', {'id': app.user.id }) }}">Retour</a>
        </button>  

    </div>


{% endblock %}


{# MODAL AFFICHER ADRESSE #}

<!-- Button trigger modal -->
    {# <button type="button" data-bs-toggle="modal" data-bs-target="#staticBackdrop"> Voir </button> #}
    <!-- Modal -->
    {# <div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">

                <div class="modal-header">
                    <h1 class="modal-title fs-5" id="staticBackdropLabel">Mon adresse</h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>

                
                <div class="modal-body">
                    <div class="bloc_card_body">
            <div class="bloc_card">

                <div class="bloc_card_header">
                    <h4> Ajouter une adresse </h4>      
                </div>

                {{ form_start(form) }}

                    <div class="bloc_card_add">
                        <p>Numéros</p>
                        {{ form_row( form.numAdrss, {label: ' '} ) }}  
                    </div>

                    
                    <div class="bloc_card_add">
                        <p>Rue</p>
                        {{ form_row( form.rueAdrss, {label: ' '} ) }}
                    </div>

                    
                    <div class="bloc_card_add">
                        <p>
                            Complément d'addresse <br>
                            <small>(Résidence, Batiment, Appartement)</small>
                        </p>                    
                        {{ form_row( form.complementAdrss, {label: ' '} ) }}
                    </div>

                    
                    <div class="bloc_card_add">
                        <p>Ville</p>
                        {{ form_row( form.villeAdrss, {label: ' '} ) }}
                    </div>

                    
                    <div class="bloc_card_add">
                        <p>Code postal</p>
                        {{ form_row( form.codePostalAdrss, {label: ' '} ) }}
                    </div>

                    <div class="bloc_card_add">
                        <p>Pays</p>
                        {{ form_row( form.paysAdrss, {label: ' '} ) }} 
                    </div>

                    
                    <button>
                        {{ button_label|default('Enregistrer') }}
                    </button>            
                
                {{ form_end(form) }}
                    </div>
                </div>
                
                <div class="modal-footer bloc_card_edit_footer">
                    <button type="button" data-bs-dismiss="modal">Retour</button>
                    <button type="button" >
                        <a href="{{ path('app_adresses_edit', {'id': user.id, 'id_adrss': adresse.id}) }}">Modifier</a>
                    </button>
                </div>
            </div>
        </div>
    </div> #}
