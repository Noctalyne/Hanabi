{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}


{% block body %}

    <div class="container w-75 d-flex flex-column pt-3 pb-3">

		<div class="container  mt-4 mb-3 pt-3 pb-3 border rounded  shadow-sm">

			<h3> Pour continuer</h3>
				
			<div class="d-flex flex-row justify-content-between">

				{# <div class="d-flex align-items-center text-center col-4 ">
					<h3>
						Pour continuer sur cette page connectez-vous ou rejoignez nous en cliquant 
						<a href="{{ path('app_register') }}"> ici</a>
					</h3>
				</div> #}

				<form method="post" class=" container w-75 pt-3 pb-3">
					{# {% if error %}
						<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
					{% endif %} #}

					{% if app.user %}
						{% if app.user.clients == null %}
							<div class="mb-3">
								Encore quelques étapes avant d'accéder à se services, vous devez rensseigner vos
							<a class="" href="{{ path('app_user_create_client' , {'user_id': app.user.id }) }}">infos perso</a>
							</div>
						{% endif %}
						
					{% else %}
						<!--div principal qui modifie la largeur du formulaire
						<div class="container w-75 d-flex flex-column justify-content-center text-center w-75"> -->

							<h4 class="mb-3 font-weight-normal"> Connectez-vous</h4>

							{# <div class="p-2">
								<div class="form-floating p-2 ">
									<label for="inputEmail">Email : </label>
									<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
								</div>

								<div class="form-floating p-2"> 
									<label for="inputPassword">Mot de passe :</label>
									<input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
								</div>

								<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
							</div>
							
							<div class="d-flex justify-content-around mt-3">
								<button class="btn btn-lg btn-secondary" type="submit">Connexion</button>
							</div>
						</div> #}
					{% endif %}
				</form>

				{# <div>
					<h3>
						- ou -
					</h3>
				</div> #}

				<div class="d-flex align-items-center text-center col-4 ">
					<h3>
						rejoignez nous en cliquant<a href="{{ path('app_register') }}"> ici</a>
					</h3>
				</div>



			</div>
    	</div>
	</div>
	

{% endblock %}
