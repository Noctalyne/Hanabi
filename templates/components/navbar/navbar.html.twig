{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('./rework_css/component_navbar.css') }}" />
{% endblock %}

<div class="container">

    <div class="navigation_bar">

        <a href="{{ path('app_accueil') }}">
            <img src="{{ asset('images/logo/grp_marque.png') }}" alt="Logo Hanabi" />
        </a>

        <nav class="w-100 navigation_link">
            <a href="{{ path('app_accueil') }}">Accueil</a>
            <a href="{{ path('app_shop')}}">Boutique</a>
            
            {# {% if app.user %}
                <a href="{{ path('app_formulaire_demande_produit_show_liste', {'id': app.user.id}) }}">
                    Contact
                </a>
            {% else %} renvoie sur la page qui informe du besoin de connexion #}
                <a  href="{{ path('app_contact')}}">Contact</a>
            {# {% endif %} #}
        
            {# {% if app.user.roles == 'ROLE_ADMIN'  %} #}
                <a href="{{ path('app_administration')}}">Admin</a>
            {# {% endif %} #}


            <form>
                <div class="input-group border rounded texte-color">
                    <input type="text" class="form-control form-control" placeholder="produit, licence...">
                    <div class="input-group-text">
                        <img src="{{ asset('images/icones/search.png') }}" alt="" width="20px" height="20px">
                    </div>
                </div>
            </form>

            {% if app.user %}
                <div class="d-flex flew-row justify-content-between">
                    <div>{% include './components/panier/_dropdown.html.twig' %}</div>{# Icone Pannier #}
                    <div>{% include './components/navbar/dropdown_account.html.twig' %}</div>
                    
                    
                </div>

            {% else %}

                <div class="menu_connect">
                    <button>
                        <a style="font-size:15px;" href="{{ path('app_login') }}">Connexion / Inscription</a>
                    </button>    
                </div>
            {% endif %}

        </nav>

    </div>


</div>