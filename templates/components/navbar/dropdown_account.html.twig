{# <div class="dropdown-center d-inline-flex justify-content-center account_dropdown"> #}
{# {% %} #}

<div class="dropdown-center account_dropdown">

    <button id="menuProfil" class="dropdown-toggle dropdown-toggle-split show" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
        <img id="img_profil" src="{{ asset('images/icones/user_black_line.png') }}" alt="Logo profil" class="m-1" width="40px"height="40px">
    </button>


    <div class="dropdown-menu dropdown-menu-end" aria-labelledby="menuProfil">

        <h6 class="dropdown-header">Bonjour, {{ app.user.username }}</h6> {#  #}

        <div class="dropdown-divider"></div>

        <a class="dropdown-item" href="{{ path('app_user_show', { id : app.user.id }) }}">Mon compte</a>

        <div class="dropdown-divider"></div>

        {# <a href="{{ path('app_formulaire_demande_produit_show_liste', {'id': app.user..id}) }}">Mes formulaires</a> #}

        <div class="dropdown-divider"></div>

        {# <a href="{{ path('app_formulaire_demande_produit_show_liste', {'id': app.user.clients.id}) }}"> #}
            <img src="{{ asset('images/icones/heart_1.png') }}" alt="Liste des favoris" class="m-1" width="30px" height="30px">
            Mes Favoris
        {# </a> #}


        <div class="dropdown-divider"></div>

        <a href="{{ path('app_formulaire_demande_produit_show_liste', {'id': app.user.id}) }}">Mes Formulaires</a>

        <div class="dropdown-divider"></div>

        <a class="dropdown-item" href="{{ path('app_commandes_index') }}">Mes commandes</a>

        <div class="dropdown-divider"></div>

        <form class="dropdown-item" method="post" action="{{ path('app_logout') }}">
            <button type="submit" class="logout nav-link"> Déconnexion </button>
        </form>


    </div>
</div>
